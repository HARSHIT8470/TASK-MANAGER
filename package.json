{
  "name": "task-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Task Management System monorepo",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  }
}
